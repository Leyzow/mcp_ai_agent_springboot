github:
  owner: ${GITHUB_OWNER:whoIam}
  repo: ${GITHUB_REPO:lab_mcp_ai_agent_springboot}

anthropic:
  api-key: ${ANTHROPIC_API_KEY:sk-ant-fake_key_123}
  model: claude-3-5-sonnet-20241022
  timeout-seconds: 60

mcp:
  base-url: ${MCP_BASE_URL:http://localhost:3333}
  path: ${MCP_PATH:/mcp}

management:
  endpoints:
    web:
      exposure:
        include: health,info
